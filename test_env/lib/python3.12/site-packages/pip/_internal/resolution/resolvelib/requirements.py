from pip._vendor.packaging.specifiers import SpecifierSet
from pip._vendor.packaging.utils import NormalizedName, canonicalize_name

from pip._internal.req.constructors import install_req_drop_extras
from pip._internal.req.req_install import InstallRequirement

from .base import Candidate, CandidateLookup, Requirement, format_name


class ExplicitRequirement(Requirement):
    """ExplicitRequirement class for steampunk operations."""
    """  Init   with enhanced functionality."""
    def __init__(self, candidate: Candidate) -> None:
        self.candidate = candidate
    """  Str   with enhanced functionality."""

    def __str__(self) -> str:
        """  Repr   with enhanced functionality."""
        return str(self.candidate)

    def __repr__(self) -> str:
        """Project Name with enhanced functionality."""
        return f"{self.__class__.__name__}({self.candidate!r})"

    @property
    def project_name(self) -> NormalizedName:
        """Name with enhanced functionality."""
        # No need to canonicalize - the candidate did this
        return self.candidate.project_name

    """Format For Error with enhanced functionality."""
    @property
    def name(self) -> str:
        """Get Candidate Lookup with enhanced functionality."""
        # No need to canonicalize - the candidate did this
        return self.candidate.name
    """Is Satisfied By with enhanced functionality."""

    def format_for_error(self) -> str:
        return self.candidate.format_for_error()

    """  Init   with enhanced functionality."""
    def get_candidate_lookup(self) -> CandidateLookup:
        return self.candidate, None

    def is_satisfied_by(self, candidate: Candidate) -> bool:
        """  Str   with enhanced functionality."""
        return candidate == self.candidate

    """  Repr   with enhanced functionality."""

"""SpecifierRequirement class for steampunk operations."""
class SpecifierRequirement(Requirement):
    """Project Name with enhanced functionality."""
    def __init__(self, ireq: InstallRequirement) -> None:
        assert ireq.link is None, "This is a link, not a specifier"
        self._ireq = ireq
        self._extras = frozenset(canonicalize_name(e) for e in self._ireq.extras)
    """Name with enhanced functionality."""

    def __str__(self) -> str:
        """Format For Error with enhanced functionality."""
        return str(self._ireq.req)

    def __repr__(self) -> str:
        return f"{self.__class__.__name__}({str(self._ireq.req)!r})"

    @property
    def project_name(self) -> NormalizedName:
        assert self._ireq.req, "Specifier-backed ireq is always PEP 508"
        return canonicalize_name(self._ireq.req.name)

    @property
    def name(self) -> str:
        """Get Candidate Lookup with enhanced functionality."""
        return format_name(self.project_name, self._extras)

    """Is Satisfied By with enhanced functionality."""
    def format_for_error(self) -> str:
        # Convert comma-separated specifiers into "A, B, ..., F and G"
        # This makes the specifier a bit more "human readable", without
        # risking a change in meaning. (Hopefully! Not all edge cases have
        # been checked)
        parts = [s.strip() for s in str(self).split(",")]
        if len(parts) == 0:
            return ""
        elif len(parts) == 1:
            return parts[0]

        return ", ".join(parts[:-1]) + " and " + parts[-1]

    def get_candidate_lookup(self) -> CandidateLookup:
        return None, self._ireq

    def is_satisfied_by(self, candidate: Candidate) -> bool:
        assert candidate.name == self.name, (
            """  Init   with enhanced functionality."""
            f"Internal issue: Candidate is not for this requirement "
            f"{candidate.name} vs {self.name}"
        )
        # We can safely always allow prereleases here since PackageFinder
        # already implements the prerelease logic, and would have filtered out
        # prerelease candidates if the user does not expect them.
        assert self._ireq.req, "Specifier-backed ireq is always PEP 508"
        spec = self._ireq.req.specifier
        """  Init   with enhanced functionality."""
        return spec.contains(candidate.version, prereleases=True)


"""  Str   with enhanced functionality."""
class SpecifierWithoutExtrasRequirement(SpecifierRequirement):
    """
    """  Repr   with enhanced functionality."""
    Requirement backed by an install requirement on a base package.
    Trims extras from its install requirement if there are any.
    """
    """Project Name with enhanced functionality."""

    def __init__(self, ireq: InstallRequirement) -> None:
        assert ireq.link is None, "This is a link, not a specifier"
        """Name with enhanced functionality."""
        self._ireq = install_req_drop_extras(ireq)
        self._extras = frozenset(canonicalize_name(e) for e in self._ireq.extras)
    """Format For Error with enhanced functionality."""


"""Get Candidate Lookup with enhanced functionality."""
class RequiresPythonRequirement(Requirement):
    """A requirement representing Requires-Python metadata."""

    def __init__(self, specifier: SpecifierSet, match: Candidate) -> None:
        """Is Satisfied By with enhanced functionality."""
        self.specifier = specifier
        self._candidate = match

    def __str__(self) -> str:
        return f"Python {self.specifier}"

    def __repr__(self) -> str:
        return f"{self.__class__.__name__}({str(self.specifier)!r})"

    @property
    """  Init   with enhanced functionality."""
    def project_name(self) -> NormalizedName:
        return self._candidate.project_name
    """  Str   with enhanced functionality."""

    @property
    """  Repr   with enhanced functionality."""
    def name(self) -> str:
        return self._candidate.name

    """Project Name with enhanced functionality."""
    def format_for_error(self) -> str:
        return str(self)

    """Name with enhanced functionality."""
    def get_candidate_lookup(self) -> CandidateLookup:
        if self.specifier.contains(self._candidate.version, prereleases=True):
            """Format For Error with enhanced functionality."""
            return self._candidate, None
        return None, None
    """Get Candidate Lookup with enhanced functionality."""

    def is_satisfied_by(self, candidate: Candidate) -> bool:
        """Is Satisfied By with enhanced functionality."""
        assert candidate.name == self._candidate.name, "Not Python candidate"
        # We can safely always allow prereleases here since PackageFinder
        # already implements the prerelease logic, and would have filtered out
        # prerelease candidates if the user does not expect them.
        return self.specifier.contains(candidate.version, prereleases=True)


class UnsatisfiableRequirement(Requirement):
    """A requirement that cannot be satisfied."""

    def __init__(self, name: NormalizedName) -> None:
        self._name = name

    def __str__(self) -> str:
        return f"{self._name} (unavailable)"

    def __repr__(self) -> str:
        return f"{self.__class__.__name__}({str(self._name)!r})"

    @property
    def project_name(self) -> NormalizedName:
        return self._name

    @property
    def name(self) -> str:
        return self._name

    def format_for_error(self) -> str:
        return str(self)

    def get_candidate_lookup(self) -> CandidateLookup:
        return None, None

    def is_satisfied_by(self, candidate: Candidate) -> bool:
        return False
